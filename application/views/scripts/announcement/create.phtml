<?php
/*
	if(isset($this -> message)) echo $this -> message;
	if(!$this -> onlyMessage) {
		if($this -> selectCategory) {
			$form = new Zend_Form();
			$form -> setAction($this -> url(array('controller' => 'announcement', 'action' => 'create')));
			$form -> setMethod('get');
			$categoryList = new Zend_Form_Element_Select('category_id');
			$categoryList -> setLabel('Kategoria:');
			foreach($this -> categories as $category) {
				$categoryList -> addMultiOption($category -> ID, $category -> name);
			}
			$form -> addElement($categoryList);
			$form -> addElement('submit', 'Wybierz');
		} else {
			$form = new Zend_Form();
			$form -> setAction($this -> url(array('controller' => 'announcement', 'action' => 'create')) . '?category_id=' . $_GET['category_id']);
			$form -> setMethod('post');
			$title = new Zend_Form_Element_Text('title');
			$title -> setLabel('Tytuł:');
			$form -> addElement($title);
			$content = new Zend_Form_Element_Textarea('content');
			$content -> setLabel('Treść:');
			$content -> setAttribs(array('rows' => '15', 'cols' => '60'));
			$form -> addElement($content);
			$form -> addElement('submit', 'Dodaj');
		}
		echo $form -> render();
	}
*/
?>

Wybierz kategorię: <select name="catID">
